============
Installation
============

At the command line::

    $ easy_install cached-property

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cached-property
    $ pip install cached-property